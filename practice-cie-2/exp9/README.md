>mkdir exp9
>cd exp9
>New-Item app.js 
>New-Item logstash.conf 
>New-Item docker-compose.yml
>npm init -y
>npm install express 
>npm install net

>docker-compose up --build
>node app.js

# open localhost:5601